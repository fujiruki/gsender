# Normalize line endings to LF on checkout (including Windows CI/dev machines).
* text=auto eol=lf

cypress/dashboard/report/index.html merge=ours

# Windows batch files require CRLF or they fail to execute on double-click.
*.bat text eol=crlf
