# 06. 変更履歴

仕様変更の経緯を記録する。コードの変更履歴（git log）ではなく、仕様の変更理由を残す場所。

| 日付 | 変更内容 | 理由 |
|:--|:--|:--|
| 2026-09-15 | プロジェクト開始。Sienci-Labs/gsenderをfujiruki/gsenderへフォーク | 本家に日本語UIが無く、issue #877でメンテナーが「i18n再整備は将来だが今は優先度低い」と明言したため、フォークで独自対応する方針に決定 |
| 2026-09-15 | issue #877にフォーク方針をコメントで報告済み | 経過共有を宣言していたため |
| 2026-09-15 | fable(Claude)へi18n設計を相談し、「自前t()実装 + 英文キー方式」を提案として受領 | react-i18nextはi18next v15→v23+のアップグレードが必要でサーバー側に波及するリスクがあり、規模(英日2言語)にも見合わないため |
| 2026-09-15 | kaigi(Sonnet)で設計レビューを実施。結論「条件付きでYes」 | 主な懸念は「本家の英文変更で翻訳が静かにフォールバックする」ことの検知手段が無い点。3条件（未翻訳/孤立キー検出をM0に含める、M0完了条件に実機確認を追加、M1一括翻訳後にレビューを入れる）を付帯 |
| 2026-09-15 | fableが3条件を反映し`scripts/i18n-sync.mjs`の完成形コードを提示 | SettingsMenu.ts等のデータ定義ファイルはt()呼び出しが存在しないため、データ定義ファイル専用の抽出パターンを追加する設計上の穴を発見・修正 |
| 2026-09-15 | SdDD(仕様書駆動開発)を導入 | 行き当たりばったりの実装を避け、マイルストーンM0〜M3を仕様として明文化するため。詳細な設計議論は`docs/kaigi/2026-09-15-i18n設計レビュー.md`を参照 |
| 2026-09-15 | M0完了後、ブラウザでUI確認したところ「Disconnected」等が未翻訳のまま残存を発見。M1の当初リストが`features/`配下36ディレクトリ中20個しかカバーしていなかったことが判明し、F-02b(常時表示UI要素)を追加 | 依頼リストの機能名列挙が網羅的でなかったための抜け漏れ。今後の依頼はディレクトリ一覧との突き合わせを徹底する |
| 2026-09-15 | M1・M2ブランチをmasterへマージ(`f1b5f0350`)。`ja.json`のマージコンフリクトで、英文キー方式の弱点(kaigiで事前に懸念されていた「同一英文が異なる文脈で衝突する」問題)が実際に発生: `"Homing"`というキーが`MachineStatus.tsx`(進行中の状態表示)と`Stats/Configuration.tsx`(設定ラベル)で異なる意味を持ち衝突。「原点復帰」で統合(害が少ない方を選択、`MachineStatus.tsx`側は「アイドル」等すでに非進行形の訳も混在するため許容) | 英文キー方式の既知のトレードオフが顕在化した実例。再発した場合はコード側で文言を区別する対応(例: 進行中表示は`'Homing...'`のように区別)を検討する。`scripts/i18n-sync.mjs`にも同一キーが複数ファイルで異なる訳を要求していないかの検出は現状ない |
