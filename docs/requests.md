# 要望一覧 (requests.md)

運用開始日: 2026-09-15

新しい要望はこのファイルに追記する。
指揮AIが読み込んで仕様として詰めたら、該当内容はクリアされる。
対応履歴は [request_log.md](request_log.md) を参照。

---

## 書き方ガイド

雑でいい。思いついたことをそのまま書く。

```markdown
- ログイン機能にGoogleアカウント対応を追加してほしい
- ダッシュボードに月次売上グラフを表示したい
```

優先度をつけたい場合:
- **【高】** 業務に支障がある / バグ / 納期が迫っている
- **【中】** あると便利 / 改善要望
- **【低】** いつかやりたい / アイデア段階

---

## 要望

<!-- ここに要望を書く。指揮AIが読み込み済みの内容はクリアされる -->

- **【中】** Electron本体(`npm run electron:hot`)がWindowsで起動しない問題を直したい。`package.json`の`electron:hot`スクリプトが`bash -lc '...'`をconcurrently経由でWindows cmd.exeに渡しており、シェル互換性の問題で起動に失敗する。現状はブラウザ表示(`start-dev`)で代替しているが、本来のElectronデスクトップアプリとして起動できるようにしたい（2026-09-16、ショートカット作成の過程で発覚）
- **【中】** キーボードジョグで「Ctrl+Shift+矢印キー」等の別キーを押している間だけ移動量/速度を上げる(高速ジョグ)機能がほしい。調査したところ、`Jogging/index.tsx`に過去`JOG_SPEED_I`/`JOG_SPEED_D`という速度変更ショートカット(`=`/`-`キー)が実装されていた形跡があるが、現在はコメントアウトされ無効化されている(コールバック`this.shuttleControlFunctions.JOG_SPEED`も未定義でクラス→関数コンポーネント移行時の残骸と思われる)。現状は画面上の「精密/通常/高速」ボタンをマウスでクリックするしかない（2026-09-16、キーボードジョグの使い方を確認した際にユーザーから要望）
- **【高】** 安全インシデント: gSenderで接続後、Shift+←キーでX軸を左方向へジョグし続けたところ、X軸のリミットスイッチに到達しても停止せず、そのままリミットスイッチを物理的に破損させた(バネで飛び出るべき可動部が押し込まれたまま戻らない状態)。
  - **GRBLログ解析で判明した事実**: 接続時に`[MSG:Check Limits]`（起動時点で既にいずれかの軸のリミットピンがトリガー状態）が出ており、その後`$H`(Homing)を実行したが`ALARM:8 (Homing fail)`で**失敗**していた。ユーザーは「ホーミングはした」と認識していたが、実際にはHoming未完了のままアラームを`$X`で解除して操作を続けていた
  - 設定値は`$21=1`(Hard limits有効)`$22=1`(Homing有効)と本来安全な構成（`$20=0`Soft limits無効はHoming未完了なら無関係）
  - **最有力の根本原因（未確定・要検証）**: 事故の結果ではなく事故前からX軸リミットスイッチが機械的に固着し常時オン状態だった可能性。ハードリミットは信号の立ち上がり(新規トリガー)を検知する実装が一般的なため、常時オンのスイッチでは新たな停止トリガーがかからず、ジョグでスイッチを押し込み続けても緊急停止しなかった、という説明なら`[MSG:Check Limits]`とスイッチ破損の状況の両方に整合する
  - **要検討**: (1)Y軸・Z軸のリミットスイッチに同様の固着がないか点検（2026-09-16点検済み・異常なし。ただし「リミットスイッチを過信して動かすとまた潰れる可能性がある」ため恒久対策は別途必要）、(2)`ALARM:8`/`[MSG:Check Limits]`発生時にHoming未完了であることをgSender UIがどう警告しているか(または警告していないか)をコード調査、(3)Homing失敗・未完了状態でのジョグ操作をgSender側で制限/強警告できないか検討（2026-09-16、実機でのインシデント発生を受けてユーザーから報告、ログ解析はClaude Codeが実施。コード調査はCodexに委譲し実施済み）
  - **Codex調査結果(2026-09-16)**: `ALARM:8`はトースト＋赤い`Alarm(8)`表示で通知されるが「ホーミング未完了・現在位置は信頼できない」と明示する専用警告ではない。`[MSG:Check Limits]`はコンソールに流れるだけで一切の特別扱いなし（見落としやすい）。**核心的な欠陥**: `controller.hasHomed`は`$H`送信後、次に届いたステータスがAlarmであっても`true`にセットされうる実装(`GrblController.js:727,1913`)で、ホーミング失敗時も「成功扱い」になりうる。さらにジョグ許可判定(`canClick`/`canClickShortcut`、`Jogging/index.tsx:124,135`)はそもそも`hasHomed`を一切参照しておらず、接続状態とactiveState(`Idle`/`Jog`)だけで許可される。事故の流れ(ALARM:8→Unlock→Idle→Shift+←が素通り)はこのコードと完全に整合する。対策案は優先順位付きで7つ提示され、Codexの推奨は「①`hasHomed`の成功判定修正→②ALARM 6-9時にfalseへリセット→③ALARM:8解除前の確認モーダル」。詳細はCodexセッション`01a0a941-aad1-7683-b8c5-b3c7da4f2453`（`codex resume`で再開可）
  - **対応順序の決定(2026-09-16)**: この修正は日本語化フォーク固有ではなく本家gSender本体のロジックの問題であり、`contrib/<topic>`経由で本家へPRを検討する価値がある（ただし本家`upstream/dev`に同じ欠陥が残っているかは未確認、`integration/dev-ja`構築時に要確認）。`contrib/`は必ず最新`upstream/dev`から切る運用のため、**この安全性修正は「ブランチ戦略7項目」(下記)で`integration/dev-ja`の土台を作った後に着手する**とユーザーと合意。それまでの運用上の注意: 修正が入るまでは、Homing未完了状態でのジョグ操作を手動で避けること
- **【中】** ブランチ戦略の計画・実行がまだ未着手。AI共有庫カンガルー`2026-09-16_05_gSender_本家追従_日本語安定版_PR運用_ブランチ設計_決定記録.md`（ChatGPT＋晴樹さんの決定）で「Claude Codeに次に依頼する作業」として以下7項目が指定されており、`docs/spec/05_技術設計.md`のブランチ運用節への反映は済んだが、実作業はこれから:
  1. 現`master`の24 aheadコミットを全件分類し、決定文書のA(日本語化中核)/B(開発支援)/C(履歴のみ)/D(本家へ返せる汎用修正)分類に漏れがないか確認する
  2. 最新`upstream/dev`を基点にした`integration/dev-ja`作成手順を具体化する
  3. 日本語化の中核コミットについて、最新devへの適用難易度・変更ファイル重複・コンフリクト見込みを調査する
  4. `ja.json`1639件を新devへ移す際のキー再利用率を確認する
  5. 本家devで新たに増えたUI文字列を、日本語化機構がどう拾うべきか整理する
  6. `10e2166`のcheck-types修正を最新本家dev上で再現・検証し、本家PR候補として妥当か確認する
  7. ここまでをカンガルーへ`status: 調査`として返す
  - **この段階ではmasterの履歴改変・default branch変更・大規模merge・本家PR送信はまだ行わない**（決定文書の制約）（2026-09-16、着手忘れ防止のため記録）
